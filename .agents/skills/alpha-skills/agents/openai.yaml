interface:
  display_name: "Alpha Skills"
  short_description: "Repo-specific patterns and workflows for alpha-skills"
  default_prompt: "Use the alpha-skills repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true